name: Goliath Auto Tool System

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */8 * * *"

permissions:
  contents: write
  issues: write

concurrency:
  group: goliath-auto
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Goliath System (capture exit)
        id: goliath_run
        continue-on-error: true
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          LEADS_TOTAL: ${{ vars.LEADS_TOTAL }}
        shell: bash
        run: |
          set +e
          python -u main.py 2>&1 | tee run_log.txt
          code=${PIPESTATUS[0]}
          echo "exitcode=$code" >> "$GITHUB_OUTPUT"
          echo "Goliath exit code: $code" >> "$GITHUB_STEP_SUMMARY"
          exit 0

      - name: Commit and Push Changes (ALL files)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git config --global user.name "Goliath-Bot"
          git config --global user.email "bot@mikanntool.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Goliath: generate pages + replies + design (auto)"
            git push
          fi

      - name: Create Issue Report (ALWAYS)
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          python -u tools/report_to_issue.py || true

      - name: Upload run log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: goliath-run-log
          path: run_log.txt
